

## Plan: Store YouTube Data API v3 Key

**Single step**: Add a new Supabase secret called `YOUTUBE_DATA_API_KEY` to store the YouTube Data API v3 key. This will be available to edge functions for server-side YouTube API calls.

No code changes needed yet — just the secret. Once stored, the user will share their plan for how the /videos page should use the API.

