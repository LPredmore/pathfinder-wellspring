name: Validate Partner Scroll Behavior

on:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx eslint src/components/layout/Layout.tsx src/components/layout/Layout.test.tsx
      - run: npx tsc --noEmit
      - run: npx vitest run src/components/layout/Layout.test.tsx
      - run: npm test
      - run: npm run build
